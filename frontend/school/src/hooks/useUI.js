// This file centralizes the UI hook for easy importing across the application.
// It re-exports the useUI hook, which provides access to UI state like
// loading indicators and notifications.

export { useUI } from '../context/UIContext';
