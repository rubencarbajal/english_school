// This file centralizes the Authentication hook for easy importing.
// It re-exports the useAuth hook from the AuthContext, which provides access
// to user data, the auth token, and methods like login, logout, and register.

export { useAuth } from '../context/AuthContext';
